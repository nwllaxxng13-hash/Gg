--[[
    Kaiju Omni-Farm V1.7.3 - FOX FULL FIXED
    Blue Sky + V5 Tween Loop Fixed | Ready to Execute
]]

local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local TweenService = game:GetService("TweenService")
local TeleportService = game:GetService("TeleportService")
local HttpService = game:GetService("HttpService")
local UserInputService = game:GetService("UserInputService")

local L = Players.LocalPlayer

-- ==========================================
-- ANTI-CHEAT BYPASS
-- ==========================================
local mt = getrawmetatable(game)
local oldNamecall = mt.__namecall
local oldIndex = mt.__index
setreadonly(mt, false)

mt.__namecall = newcclosure(function(self, ...)
    local method = getnamecallmethod()
    local args = {...}
    if method == "FireServer" and not checkcaller() then
        local name = tostring(self):lower()
        if name:match("ban") or name:match("kick") or name:match("report") or name:match("anticheat") then
            return nil
        end
    end
    return oldNamecall(self, unpack(args))
end)

mt.__index = newcclosure(function(self, key)
    if not checkcaller() then
        if key == "WalkSpeed" or key == "Speed" then return 16 end
        if key == "JumpPower" then return 50 end
    end
    return oldIndex(self, key)
end)
setreadonly(mt, true)

RunService.Heartbeat:Connect(function()
    local char = L.Character
    if char then
        local hrp = char:FindFirstChild("HumanoidRootPart")
        if hrp then
            hrp.AssemblyLinearVelocity = Vector3.new(0,0,0)
            hrp.AssemblyAngularVelocity = Vector3.new(0,0,0)
            if hrp.Position.Y > 500 then
                hrp.CFrame = hrp.CFrame * CFrame.new(0, -150, 0)
            end
        end
    end
end)

-- ==========================================
-- ADMIN LIST
-- ==========================================
local AdminList = {
    ["bookgohih555"] = true, ["THEGOLOFT"] = true, ["1TAD00R11YuJ1"] = true,
    ["IMax_00123"] = true, ["7EAKHUN"] = true, ["hohome123123"] = true,
    ["Leocool1178"] = true, ["wingsgoddragon"] = true, ["GlaceSenpai21"] = true,
    ["Clericmaster11235"] = true, ["khunnapat1966"] = true, ["TAEKHUNN"] = true,
    ["mojuay00"] = true, ["bookgohih566"] = true, ["Mr_Goji09"] = true,
    ["AGN_G1"] = true, ["SolisTheAbsol"] = true, ["snipperboy978"] = true,
    ["Sentinel_Flames"] = true, ["SxpxrGxmx"] = true, ["Nixken23819"] = true,
    ["Dentalment"] = true
}

-- ==========================================
-- REMOTES
-- ==========================================
local Remotes = ReplicatedStorage:WaitForChild("Remotes")
local FireServerRemote = Remotes:WaitForChild("FireServer")
local CombatRemote = Remotes:WaitForChild("CombatRemote")
local BeamRemote = Remotes:WaitForChild("BeamRemote")

-- ==========================================
-- VARIABLES
-- ==========================================
local FarmingV1Enabled = false
local FarmingV2Enabled = false
local FarmingV3Enabled = false
local FarmingV4Enabled = false
local FarmingV5Enabled = false
local AutoHopEnabled = false
local AutoHopV2Enabled = false
local AutoHopV3Enabled = false
local AutoLockBeamEnabled = false
local NoclipEnabled = false

local AutoSpaceGodzilla = false
local KaiTanStage = 1
local CurrentKaiTanName = "Gojira 1954"

local AutoDestoroyah = false
local DestoroyahStage = 1
local CurrentDestoroyahName = "Destoroyah Aggregate"

local CurrentTarget = nil
local LastTargetSwitch = 0
local TargetLockTime = 1.2
local KaijuName = "Biollante"
local AdminHopCount = 0

local SelectedSkills = {
    Attack1 = true, Attack2 = true, Attack3 = true,
    Attack4 = false, Attack5 = false
}

local currentTween = nil

-- ==========================================
-- CONFIG
-- ==========================================
local ConfigFile = "FoxKaijuConfig.json"

local function SaveConfig()
    pcall(function()
        if writefile then
            local data = {
                V1 = FarmingV1Enabled, V2 = FarmingV2Enabled, V3 = FarmingV3Enabled,
                V4 = FarmingV4Enabled, V5 = FarmingV5Enabled,
                Hop = AutoHopEnabled, HopV2 = AutoHopV2Enabled, HopV3 = AutoHopV3Enabled,
                KaijuName = KaijuName, AdminHopCount = AdminHopCount,
                AutoLockBeam = AutoLockBeamEnabled, Noclip = NoclipEnabled,
                AutoSpaceG = AutoSpaceGodzilla, KStage = KaiTanStage,
                AutoDes = AutoDestoroyah, DStage = DestoroyahStage,
                SelectedSkills = SelectedSkills
            }
            writefile(ConfigFile, HttpService:JSONEncode(data))
        end
    end)
end

local function LoadConfig()
    pcall(function()
        if readfile and isfile(ConfigFile) then
            local d = HttpService:JSONDecode(readfile(ConfigFile))
            FarmingV1Enabled = d.V1 or false
            FarmingV2Enabled = d.V2 or false
            FarmingV3Enabled = d.V3 or false
            FarmingV4Enabled = d.V4 or false
            FarmingV5Enabled = d.V5 or false
            AutoHopEnabled = d.Hop or false
            AutoHopV2Enabled = d.HopV2 or false
            AutoHopV3Enabled = d.HopV3 or false
            KaijuName = d.KaijuName or "Biollante"
            AdminHopCount = d.AdminHopCount or 0
            AutoLockBeamEnabled = d.AutoLockBeam or false
            NoclipEnabled = d.Noclip or false
            AutoSpaceGodzilla = d.AutoSpaceG or false
            KaiTanStage = d.KStage or 1
            AutoDestoroyah = d.AutoDes or false
            DestoroyahStage = d.DStage or 1
            if d.SelectedSkills then SelectedSkills = d.SelectedSkills end
        end
    end)
end
LoadConfig()

-- ==========================================
-- UTILITY
-- ==========================================
local function IsTargetValid(target)
    if not target or not target.Parent then return false end
    local char = target.Character
    if not char then return false end
    local hum = char:FindFirstChild("Humanoid")
    local hrp = char:FindFirstChild("HumanoidRootPart")
    return hum and hum.Health > 0 and hrp
end

local function GetNearestPlayerV4()
    local myHRP = L.Character and L.Character:FindFirstChild("HumanoidRootPart")
    if not myHRP then return nil end
    local nearest, minDist = nil, math.huge
    for _, p in ipairs(Players:GetPlayers()) do
        if p ~= L and IsTargetValid(p) then
            local dist = (p.Character.HumanoidRootPart.Position - myHRP.Position).Magnitude
            if dist < minDist then
                minDist = dist
                nearest = p
            end
        end
    end
    return nearest
end

local function TweenToTarget(hrp, targetCFrame)
    if currentTween then currentTween:Cancel() end
    local dist = (hrp.Position - targetCFrame.Position).Magnitude
    local timeToReach = math.clamp(dist / 280, 0.08, 0.6)
    local tweenInfo = TweenInfo.new(timeToReach, Enum.EasingStyle.Linear)
    currentTween = TweenService:Create(hrp, tweenInfo, {CFrame = targetCFrame})
    currentTween:Play()
end

-- ==========================================
-- SERVER HOP
-- ==========================================
local function ServerHop()
    TeleportService:Teleport(game.PlaceId, L)
end

local function EmergencyHop(adminName)
    AdminHopCount = AdminHopCount + 1
    SaveConfig()
    TeleportService:Teleport(game.PlaceId, L)
end

-- ==========================================
-- WINDUI
-- ==========================================
local WindUI
do
    local ok, result = pcall(function()
        return loadstring(game:HttpGet("https://raw.githubusercontent.com/Footagesus/WindUI/main/dist/main.lua"))()
    end)
    if ok then WindUI = result else
        WindUI = require(ReplicatedStorage:WaitForChild("WindUI"):WaitForChild("Init"))
    end
end

local Window = WindUI:CreateWindow({
    Title = "Kaiju Omni-Farm",
    Author = "Fox for Jack",
    Icon = "solar:wind-bold",
    Theme = "Dark",
    ToggleKey = Enum.KeyCode.RightControl,
})

Window:Tag({ Title = "v1.7.3 - Fixed", Color = "ElementBackground" })

local FarmTab = Window:Tab({ Title = "Auto Farm", Icon = "crosshair" })
local KaiTanTab = Window:Tab({ Title = "KaiTan", Icon = "rocket" })
local HopTab = Window:Tab({ Title = "Server Hop", Icon = "globe" })
local MiscTab = Window:Tab({ Title = "Misc", Icon = "wrench" })

-- Farm Settings
local FarmSettings = FarmTab:Section({ Title = "Target Settings" })
FarmSettings:Input({
    Title = "Kaiju Name",
    Placeholder = "Biollante",
    Value = KaijuName,
    Callback = function(v) KaijuName = v; SaveConfig() end
})

FarmSettings:Toggle({
    Title = "Auto Lock Beam",
    Value = AutoLockBeamEnabled,
    Callback = function(v) AutoLockBeamEnabled = v; SaveConfig() end
})

-- Skills
local SkillSection = FarmTab:Section({ Title = "Skills" })
for i=1,5 do
    local name = "Attack"..i
    SkillSection:Toggle({
        Title = "Loop "..name,
        Value = SelectedSkills[name],
        Callback = function(v) SelectedSkills[name] = v; SaveConfig() end
    })
end

-- Farm Modes
local AttackSection = FarmTab:Section({ Title = "Farm Modes" })
AttackSection:Toggle({ Title = "FARM V1 (Warp)", Value = FarmingV1Enabled, Callback = function(v) FarmingV1Enabled = v; SaveConfig() end })
AttackSection:Toggle({ Title = "FARM V2 (Rapid)", Value = FarmingV2Enabled, Callback = function(v) FarmingV2Enabled = v; SaveConfig() end })
AttackSection:Toggle({ Title = "FARM V3 (Lock)", Value = FarmingV3Enabled, Callback = function(v) FarmingV3Enabled = v; SaveConfig() end })
AttackSection:Toggle({ Title = "FARM V4 (Skip Killed)", Value = FarmingV4Enabled, Callback = function(v) FarmingV4Enabled = v; SaveConfig() end })
AttackSection:Toggle({ Title = "FARM V5 (Tween - FIXED)", Value = FarmingV5Enabled, Callback = function(v) FarmingV5Enabled = v; SaveConfig() end })

-- KaiTan Tab
local KaiTanSection = KaiTanTab:Section({ Title = "Space Godzilla" })
KaiTanSection:Toggle({
    Title = "Auto Space Godzilla",
    Value = AutoSpaceGodzilla,
    Callback = function(v) AutoSpaceGodzilla = v; if v then FarmingV5Enabled = true end; SaveConfig() end
})

local DesSection = KaiTanTab:Section({ Title = "Destoroyah" })
DesSection:Toggle({
    Title = "Auto Destoroyah",
    Value = AutoDestoroyah,
    Callback = function(v) AutoDestoroyah = v; if v then FarmingV5Enabled = true end; SaveConfig() end
})

-- Hop Tab
local HopSection = HopTab:Section({ Title = "Hop Settings" })
HopSection:Toggle({ Title = "Auto Hop V1 (180s)", Value = AutoHopEnabled, Callback = function(v) AutoHopEnabled = v; SaveConfig() end })
HopSection:Toggle({ Title = "Auto Hop V2 (60s)", Value = AutoHopV2Enabled, Callback = function(v) AutoHopV2Enabled = v; SaveConfig() end })
HopSection:Toggle({ Title = "Auto Hop V3 (30s)", Value = AutoHopV3Enabled, Callback = function(v) AutoHopV3Enabled = v; SaveConfig() end })

-- Misc
local MiscSection = MiscTab:Section({ Title = "Misc" })
MiscSection:Toggle({
    Title = "Noclip",
    Value = NoclipEnabled,
    Callback = function(v) NoclipEnabled = v; SaveConfig() end
})

-- ==========================================
-- V5 FARM LOOP (FIXED)
-- ==========================================
task.spawn(function()
    while true do
        task.wait(0.08)
        if not FarmingV5Enabled then continue end

        local myChar = L.Character
        local myHRP = myChar and myChar:FindFirstChild("HumanoidRootPart")
        if not myHRP then continue end

        if not IsTargetValid(CurrentTarget) or (tick() - LastTargetSwitch > TargetLockTime) then
            CurrentTarget = GetNearestPlayerV4()
            if CurrentTarget then LastTargetSwitch = tick() end
        end

        if CurrentTarget and IsTargetValid(CurrentTarget) then
            local targetHRP = CurrentTarget.Character.HumanoidRootPart
            local dist = (targetHRP.Position - myHRP.Position).Magnitude

            if dist > 8 then
                TweenToTarget(myHRP, targetHRP.CFrame)
            else
                myHRP.CFrame = targetHRP.CFrame * CFrame.new(0, 4, -6)
            end

            for skill, enabled in pairs(SelectedSkills) do
                if enabled then
                    pcall(function() CombatRemote:FireServer("UseSkill", skill) end)
                end
            end
        end
    end
end)

-- Noclip
RunService.Stepped:Connect(function()
    if NoclipEnabled and L.Character then
        for _, part in ipairs(L.Character:GetDescendants()) do
            if part:IsA("BasePart") then part.CanCollide = false end
        end
    end
end)

-- Spawn Loop
task.spawn(function()
    while true do
        task.wait(2.5)
        if FarmingV5Enabled or FarmingV4Enabled or FarmingV3Enabled or FarmingV2Enabled or FarmingV1Enabled then
            pcall(function()
                local target = KaijuName
                if AutoSpaceGodzilla then target = CurrentKaiTanName end
                if AutoDestoroyah then target = CurrentDestoroyahName end
                FireServerRemote:FireServer("Spawn", target, "Hong Kong")
            end)
        end
    end
end)

-- Auto Lock Beam
task.spawn(function()
    while true do
        task.wait(0.1)
        if AutoLockBeamEnabled and CurrentTarget and IsTargetValid(CurrentTarget) then
            local hrp = CurrentTarget.Character.HumanoidRootPart
            pcall(function() BeamRemote:FireServer(hrp.Position) end)
        end
    end
end)

print("=== FOX FULL SCRIPT V1.7.3 LOADED ===")
print("Blue sky & Tween loop fixed")

bazinga.
